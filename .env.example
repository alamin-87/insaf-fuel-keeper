# MongoDB Atlas — copy to .env and fill in
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?appName=D360CRM
MONGODB_DB=insaf_gas_corp

# Session (min 32 chars in production)
SESSION_SECRET=change-me-to-a-long-random-secret-32+

# Bootstrap login (used if Mongo appUsers is empty / unreachable)
AUTH_USER=operator
AUTH_PASSWORD=insaf123
AUTH_DISPLAY_NAME=Operator
